using UnityEngine;

namespace Scenes.Workshops.Models
{
    public class MoldElementCtlr : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
